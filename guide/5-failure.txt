5. When something fails

A failure is a value too, not a crash. A line that fails stops, changes nothing, and says why, with the kind of failure beside the message.

  read missing.txt

A command called wrongly, with an argument missing, one too many or a flag it does not have, fails the same way, and under the error the page shows that command's help, in a panel labelled help: what help with the command's name would say. Run read on its own, with no file named, to see it. A file that is not there is not a wrong call, so read missing.txt shows no help. A name the terminal does not know says which command it probably meant, so cd says to use in.

else runs something instead when the line before it fails:

  read missing.txt else echo "no such file"

try keeps the failure as a value, so it can be looked at:

  try read missing.txt | set problem
  $problem.kind
  $problem.message

?? gives a default when a stage answered nothing at all:

  first (ls | where $row.kind eq view) ?? "no views yet"

A line is all or nothing. If any part of it fails, nothing it did is kept.

Next: read guide/6-undo.txt
